# Migration `20210115163727-initial-version`

This migration has been generated by Noah Stotland at 1/15/2021, 11:37:27 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."EnumStatus" AS ENUM ('new', 'pending', 'onHold', 'ongoing', 'done')

CREATE TABLE "User" (
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "firstName" TEXT,
    "id" TEXT NOT NULL,
    "lastName" TEXT,
    "password" TEXT NOT NULL,
    "roles" TEXT[],
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "username" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Project" (
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "description" TEXT,
    "dueDate" TIMESTAMP(3),
    "id" TEXT NOT NULL,
    "name" TEXT,
    "startDate" TIMESTAMP(3),
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Task" (
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "estimation" INTEGER,
    "id" TEXT NOT NULL,
    "startDate" TIMESTAMP(3) NOT NULL,
    "status" "EnumStatus" NOT NULL,
    "title" TEXT NOT NULL,
    "updatedAt" TIMESTAMP(3) NOT NULL,
    "userId" TEXT,
    "projectId" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "Documents" (
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" TEXT NOT NULL,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.username_unique" ON "User"("username")

ALTER TABLE "Project" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "Task" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "Task" ADD FOREIGN KEY("projectId")REFERENCES "Project"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210115163727-initial-version
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,62 @@
+datasource postgres {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  createdAt DateTime  @default(now())
+  firstName String?
+  id        String    @id @default(cuid())
+  lastName  String?
+  password  String
+  roles     String[]
+  updatedAt DateTime  @updatedAt
+  username  String    @unique
+  Project   Project[]
+  Task      Task[]
+}
+
+model Project {
+  createdAt   DateTime  @default(now())
+  description String?
+  dueDate     DateTime?
+  id          String    @id @default(cuid())
+  name        String?
+  owner       User      @relation(fields: [userId], references: [id])
+  startDate   DateTime?
+  updatedAt   DateTime  @updatedAt
+  Task        Task[]
+  userId      String
+}
+
+model Task {
+  assignedTo User?      @relation(fields: [userId], references: [id])
+  createdAt  DateTime   @default(now())
+  estimation Int?
+  id         String     @id @default(cuid())
+  project    Project    @relation(fields: [projectId], references: [id])
+  startDate  DateTime
+  status     EnumStatus
+  title      String
+  updatedAt  DateTime   @updatedAt
+  userId     String?
+  projectId  String
+}
+
+model Documents {
+  createdAt DateTime @default(now())
+  id        String   @id @default(cuid())
+  updatedAt DateTime @updatedAt
+}
+
+enum EnumStatus {
+  new
+  pending
+  onHold
+  ongoing
+  done
+}
```


